import React from 'react';
import { Upload } from 'react-bootstrap-icons';
import styles from './SetFilterIcon.module.css';

export default function SetFilterIcon() {
  return <Upload class={styles.SetFilterIcon}/>;
}
